# Frontend env (Vite)
# Keep this as /api in development so it works from other devices via Vite proxy
VITE_API_BASE_URL=/api

# Optional: where Vite should proxy /api during local development
VITE_API_PROXY_TARGET=http://localhost:4000
